# 문맥 근거와 검색 진단 구현

## 범위와 구현

기준 revision `e1941da`에서 승인된 문맥 근거 설계와 계획을 구현했다. 특정 상품명·지급일 표현을 업무 코드에 넣지 않았다.
본래 main checkout을 사용했고 새 worktree·DB·계정·영속 업로드 자료를 만들지 않았다.

- 요청별 query embedding 재사용, BM25/dense 원점수 보존. RRF 순위 계산은 유지.
- 모든 적격 문장과 문맥을 대상으로 원문 단위 그룹을 선택. 그룹·문장 수·문자 예산, 출처 동일성, 충돌 근거 보존 검사.
- 신규 prompt/profile만 문맥 입력 사용. 기존 prompt의 입력과 임계값은 유지.
- 생성 입력에 문서/버전/projection/element/offset/section/표 셀 위치를 보존. 모든 생성 근거를 응답에 매핑.
- 대화에서 선택형 진단: BM25/dense/RRF, 문장·문맥 cosine, 선택/제외 사유, 단계 시간. 미계산과 0 구분.
- 추가 인용 원문 열기, context 하이라이트, 누락 인용 오류. 진단 계산 실패는 답변 선택과 분리.

## 검증

- 관련 backend 1,177 passed / 4 skipped. 테스트용 합성 `AI_WORKSHOP_SECRET_KEY`로 실행; 실제 서비스 secret은 사용하지 않았다.
- 최종 중복 JSON 키 거부 보완 후 provider/abstention 77 passed. 해당 회귀의 수정 전 실패와 수정 후 통과 확인.
- conversation/source viewer/source route 8파일49 tests passed.
- Python mypy 189 source files, Ruff, TypeScript, ESLint 통과. OpenAPI 타입은 생성기로 갱신.
- 독립 검토 89 tests passed. 기존 중요 지적(적격 문장 누락, 표 위치 누락, 비Codex 근거 부족 처리) 해소.
- 다양한 질문의 selector fixture는 계약 검증이며, 가짜 임베딩 점수로 실모델 의미 정확도를 주장하지 않는다.

## 본래 환경 읽기 전용 비교

기존 승인 합성 자료만 대상으로 실행했다. 구성 v3 `6b463754-c7c3-4b2c-ab0d-af87abbbb560`,
projection `92837dab-15d5-41d5-a6ed-877d9adea1f4`, 문서 `16e67e14-612a-48bf-87f7-98f855ee30a6`.
기존 등록 로컬 임베딩/검색 정책을 재사용했고 의미 임계값 0.9를 유지했다.
BM25 비교는 동일 검색 설정에서 dense 분기만 제외한 메모리 내 구성이다. DB에 후보 구성이나 평가 성공을 기록하지 않았다.
스크립트는 `.local-data/backups/original-rag-cutover-20260920/diagnose_context_search.py`.

| 질문/모드 | 기존 생성 근거의 지급일 문장 | 후보 문맥의 지급일 문장 | 후보 단위 수 | 검색+진단+후보 선택 시간 |
| --- | --- | --- | --- | --- |
| 지급일 hybrid 첫 요청 | 0/1 | 1/1 | 10 | 23.865초 |
| 지급일 hybrid 반복 | 0/1 | 1/1 | 10 | 3.634초 |
| 지급일 BM25 첫 비교 | 0/1 | 1/1 | 10 | 2.598초 |
| 지급일 BM25 반복 | 0/1 | 1/1 | 10 | 3.947초 |
| 무관 질문 hybrid | 해당 없음 | 해당 없음 | 0 | 3.655초 |
| 무관 질문 BM25 | 해당 없음 | 해당 없음 | 0 | 0.064초 |

지급일 문장의 기대 ID는 `1db970b9-e3fa-4e80-8430-30043a3eda45`다. 핵심 문맥 cosine은 0.882409,
keyword coverage는 0.666667이었다. 다른 상품 문맥도 함께 선택되므로 생성 단계의 대상 구분 검증이 필요하다.
상품명 질문도 실행했으나 기대 문장을 찾는 진단 스크립트 조건이 일치하지 않아 해당 질문의 recall은 판정하지 않았다.
원문/질문 생성은 끈 로컬 비교이며 **LLM 답변 속도나 정답률 측정이 아니다**.
반복 측정은 같은 로드된 임베딩 인스턴스를 재사용하므로 새 HTTP 요청의 지연 개선 수치로 해석하지 않는다.

## 적용 상태와 남은 게이트

아래는 최초 코드 인계 시점의 상태다. 후속 [세션 구현 기록](2026-09-21-rag-conversation-sessions.md)에서
본래 환경의 실제 연결·평가·승인 게이트를 통과한 구성v4를 연결2로 활성화했다.
지급일7영업일·상품명과 원문 인용 및 무관 질문 거절을 확인했다. 넓은 질문 품질 평가와 브라우저 인용 확인은 남아 있다.

코드 독립 검토에는 남은 중요 차단 사항이 없다. 본래 브라우저는 로그인 화면이며 사용자에게 기존 마스터 로그인을 요청했다.
본래 API를 현재 코드로 재시작했고 API18000/프론트 경유5173 health200을 확인했다. worker·beat와 원본 자료는 유지했다.
관리 API를 통한 신규 profile 검증·실제 양성/음성 생성 평가·다대상 값 혼동 평가·구성 연결과 화면 원문 확인은 아직 미완료다.
새 프로파일 평가 실행 전에 기존 worker의 유휴 상태를 확인하고 현재 코드로 재시작해야 한다.
평가된 v3를 유지하고 신규 문맥 prompt를 기본으로 승격하지 않았다. 실사용 답변 품질 검증 완료로 보고하지 않는다.
다음 검증은 같은 기존 합성 자료에서 기간/금액/절차/조건/예외/인명/부정/무관/다대상 질문을 사용한다.
실행 성공 상태나 승인 기록을 SQL로 조작하지 않는다.
