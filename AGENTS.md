# AGENTS.md

이 파일은 AI Workshop 저장소 전체에 적용되는 Codex 작업 지침이다. 더 구체적인 하위 `AGENTS.md` 또는 `AGENTS.override.md`가 생기면 해당 디렉터리 범위에서는 하위 지침을 우선한다.

## 파일 유지 규칙

- 이 파일은 공백을 포함해 200줄 이하로 유지한다.
- 저장소 전체에 즉시 적용해야 하는 짧은 규칙만 둔다.
- 상세 배경, 예시, 절차가 필요하면 `docs/guidelines/codex/` 또는 관련 설계 문서로 옮기고 이 파일에서 링크한다.
- 같은 지침을 이 파일과 참고 문서에 중복 작성하지 않는다. `AGENTS.md`에는 요약과 정본 링크만 남긴다.
- 새 지침 때문에 200줄을 넘게 되면 추가하기 전에 기존 상세 내용을 참고 문서로 분리한다.
- 하위 디렉터리에 별도 `AGENTS.md`를 만들 때도 같은 200줄 제한과 참고 문서 분리 원칙을 적용한다.
- 참고 문서의 배치와 연결 규칙은 `docs/guidelines/codex/README.md`를 따른다.

## 작업 전 읽을 문서

작업 범위에 맞는 문서를 먼저 읽고 승인된 설계를 따른다.

- 프로젝트 목적: `docs/vision/project-vision.md`
- 현재 작업과 다음 순서: `WORKBOARD.md`
- 전체 경계와 단계: `docs/architecture/system-design.md`
- 목표 파일 구조: `docs/architecture/repository-structure.md`
- 공통 개발 규칙: `docs/guidelines/development-guidelines.md`
- 로컬 실행과 검증: `docs/runbooks/local-development.md`
- Codex 참고 문서 구조: `docs/guidelines/codex/README.md`
- RAG 작업: `docs/labs/rag/design.md`
- 기술 결정: `docs/decisions/`

설계 문서와 코드가 충돌하면 임의로 한쪽을 선택하지 않는다. 변경 의도를 확인하고 같은 작업에서 문서와 코드를 함께 갱신한다.

## 현재 프로젝트 단계

- 1단계 작업소 기반과 첫 RAG AI 검색 수직 슬라이스가 구현됐다.
- 현재 RAG 범위는 Markdown, TXT와 텍스트 PDF ingestion, 구조 청킹, BM25+dense+RRF 검색, 근거·하이라이트·원문 뷰어, 저장 구성과 평가 비교다.
- 로컬 실행, migration, owner bootstrap과 smoke 절차는 `docs/runbooks/local-development.md`만 정본으로 사용한다.
- 다음 RAG 경계는 DOCX 파서·뷰어 지원이며 그 다음은 스캔 PDF OCR이다.
- 미래 `Labs` 영역이나 이후 RAG 형식의 빈 폴더와 예제 구현을 미리 만들지 않는다.

## 아키텍처 경계

- 저장소는 React 프론트엔드와 FastAPI 백엔드를 포함하는 모듈형 모놀리스로 시작한다.
- `Platform`은 기술 공통 기능, `Labs`는 AI 기술별 기능을 담당한다.
- `Platform`은 `Labs/RAG` 구현에 의존하지 않는다.
- 서로 다른 `Labs` 영역은 직접 의존하지 않는다.
- 데이터베이스, Elasticsearch, 파일 저장소, 파서와 모델 런타임은 어댑터 뒤에 둔다.
- API 핸들러와 UI 컴포넌트에 핵심 업무 규칙을 두지 않는다.
- 실제 요구와 측정 없이 마이크로서비스로 분리하지 않는다.

## RAG 기준선

- 첫 검색 기준선은 `BM25 + bi-encoder dense retrieval + RRF`다.
- BM25 단독 결과를 비교 기준으로 유지한다.
- 색인, 검색, 생성 프로파일을 분리한다.
- 임베딩 또는 청킹 방식이 바뀌면 호환되는 새 색인 버전을 만든다.
- LLM 변경만으로 검색 색인을 다시 만들지 않는다.
- 첫 검색 순위에는 LLM을 사용하지 않는다.
- 모든 정상 검색 결과는 권한이 허용된 원문 위치로 추적할 수 있어야 한다.
- 의미 하이라이트와 정확한 문자열 일치를 UI 및 데이터에서 구분한다.

## 데이터와 보안

- 원본 문서, 메모와 실험 기록은 기본 비공개다.
- 문서 원본, 파싱 결과와 임베딩은 로컬 또는 승인된 사내 환경에서 처리한다.
- 외부 모델 API에는 공개 또는 비식별로 승인된 데이터만 전송한다.
- 권한 및 지식 공간 필터는 BM25와 벡터 검색 전에 적용한다.
- 비공개 본문, 비밀값과 개인정보를 로그, 테스트 픽스처 또는 Git에 저장하지 않는다.
- 공개 전시실과 비공개 작업소의 데이터 저장소를 직접 연결하지 않는다.

## 모델과 에이전트

- 모델명, 버전과 중요 파라미터를 업무 코드에 직접 고정하지 않는다.
- 모든 모델 실험은 데이터 스냅샷, 프로파일, 실행 환경과 평가 결과를 기록한다.
- 평가되지 않은 프로파일을 일반 검색 기본값으로 승격하지 않는다.
- 정해진 알고리즘으로 처리할 수 있는 작업에는 LLM 에이전트를 사용하지 않는다.
- 캐릭터, 서비스 작업자와 LLM 에이전트를 구분한다.
- 에이전트는 승인된 애플리케이션 도구만 사용하며 호출 사용자의 권한을 넘을 수 없다.

## 구현과 검증

- 변경 전 성공 기준과 영향을 받는 모듈을 확인한다.
- 기능 및 버그 수정은 검증 가능한 테스트와 함께 구현한다.
- Python과 TypeScript에 정적 타입 검사를 적용한다.
- 단위 테스트는 외부 모델과 네트워크 없이 실행 가능하게 유지한다.
- 검색, 저장소, 권한과 비동기 작업은 통합 테스트로 검증한다.
- 실제 민감 문서를 테스트 데이터로 사용하지 않는다.
- 완료를 주장하기 전에 관련 테스트, 타입 검사, 린트 또는 문서 검사를 실제로 실행하고 결과를 확인한다.
- 테스트 명령이 아직 정의되지 않았다면 존재하지 않는 명령을 가정하지 말고 구현 계획에서 먼저 정한다.

## 오류와 기록

- 오류를 숨기고 다른 파서나 모델로 조용히 전환하지 않는다.
- 재시도, 대체 처리와 부분 실패를 사용자 상태 및 실행 기록에 남긴다.
- 비동기 작업은 멱등성을 가져야 한다.
- Platform Asset과 RAG Projection의 READY·활성 수명주기를 구분하고 상세 기준은 `docs/labs/rag/design.md`를 따른다.
- 모델의 비공개 내부 추론을 저장하거나 실행 근거로 제시하지 않는다. 도구, 입력, 결과, 원문 근거, 상태와 소요 시간을 기록한다.

## 문서와 저장소 관리

- 작업 시작과 종료 시 `WORKBOARD.md`를 갱신하고 최근 완료 작업은 최대 5개만 유지한다.
- 중요한 공개 계약 또는 모듈 경계 변경은 구현 전에 설계 문서와 ADR을 갱신한다.
- 자유 메모, 구조화된 실험, 공식 설계와 공개 결과물을 구분한다.
- 대용량 원본, 임베딩, 모델 가중치와 생성 색인은 Git에 추가하지 않는다.
- 사용자 또는 다른 도구가 만든 관련 없는 변경을 덮어쓰거나 정리하지 않는다.
- 커밋에는 현재 작업에 해당하는 파일만 포함한다.
